import { Store } from './types';

const createStore = (/* reducer, [initialState], [enhancer] */): void/* Store */ => {
  // Ваш код
};

const applyMiddleware = (/* ...middleware */) => {
  // Ваш код
};

export { createStore, applyMiddleware };
